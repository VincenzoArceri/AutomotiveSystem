/**
 * Interfaccia Observer implementata dalle auto
 * 
 * @author Vincenzo Arceri, Matteo Calabria, Pietro Musoni, Carlo Tacchella
 */
public interface Observer {
	public void update(Packet packet);
}
